apiVersion: v1
kind: Service
metadata:
  name: {{ include "ournezt-postgres.fullname" . }}
  labels:
    {{- include "ournezt-postgres.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "ournezt-postgres.selectorLabels" . | nindent 4 }}
